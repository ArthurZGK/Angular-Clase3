import { ToFormatPhonePipe } from './to-format-phone.pipe';

describe('ToFormatPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new ToFormatPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
